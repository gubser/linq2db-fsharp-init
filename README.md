# Extension method UseFSharp not working (+ fix)

This project fails because `UseFSharp` is not found:

```
dotnet build UseFSharpNotWorking
```

This project has it fixed:

```
dotnet build UseFSharpFixed
```
